# 화면 구현 계획 — 폰 6 + 웹 2

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 시드 데이터만으로 `docs/DESIGN.md` §6의 폰 화면 6개와 웹 화면 2개가 실제 라우트에서 뜬다. `pnpm db:seed && pnpm dev` 후 로그인하면 8개 라우트가 시안대로 보인다.

**Architecture:** `packages/db/src/queries/`가 행→도메인 변환을 독점한다(모든 읽기는 core 스키마 `.parse()`로 끝난다). `apps/web`은 서버 컴포넌트가 기본이고, 상태가 있는 것만 클라이언트 컴포넌트다. 반응형은 CSS로 — 같은 라우트가 `tablet:`/`desktop:` 변형으로 레이아웃만 바꾼다.

**Tech Stack:** Next 16 App Router, React 19, Tailwind v3(토큰은 `apps/web/tailwind.config.ts`), Drizzle 0.45, zod 4, vitest 5.

**Spec:** `docs/superpowers/specs/2026-09-23-screens-design.md`

## Global Constraints

- **`any` 금지.** `@ts-ignore` 금지. 외부 입력은 zod로 파싱한다.
- **hex 금지.** 색은 `tailwind.config.ts` 토큰 클래스만 (`bg-paper`, `text-ink-muted`, `border-caution-line`, `text-verified` …). 새 색이 필요하면 `docs/DESIGN.md` §1에 있는지 먼저 확인한다.
- **터치 타깃 44px 이상, 예외 없음.** `div`에 `onClick`/`role` 금지 — 실제 `<button>`/`<a href>`/`<input>`+`<label>`. 아이콘 전용 버튼에 `aria-label`.
- **아이콘은 인라인 스트로크 SVG** (`stroke-width` 1.3~1.6, `stroke-linecap="round"`). 이모지 금지.
- **배지는 두 종류뿐**: `검증됨`(체크 원, `verified`) / `심사 전`(경고 삼각형, `caution`). **점수 숫자를 배지에 넣지 않는다.**
- 본문 칼럼은 어느 폭에서든 `max-w-prose`(660px).
- 숫자 정렬이 필요한 곳(시간, 표, 카운터)은 `tabular-nums`.
- 모든 논문에 원문 링크: `doi` > `arxivId` > `pdfUrl` 순으로 고른다.
- 브레이크포인트 별칭은 `tablet`(720px) / `desktop`(1080px). 이미 `tailwind.config.ts`에 있다.
- 커밋 메시지는 한국어, 끝에 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.
- 스테이징 금지: `.env*`, `.next/`, `next-env.d.ts`, `tsconfig.tsbuildinfo`, `.superpowers/`.
- 로컬 DB는 호스트 포트 **5433**. 컨테이너 `jogan-db`가 떠 있어야 한다(`docker compose up -d`).

## 파일 구조

```
packages/core/src/paper.ts            PaperSummary 추가
packages/db/src/
├── client.ts                         dev HMR 캐시 + 풀 설정 (수정)
└── queries/
    ├── mappers.ts                    행 → 도메인. 전부 core .parse()로 끝난다
    ├── mappers.test.ts
    ├── briefs.ts                     getTodayBrief, getLatestBrief, countStreak
    ├── papers.ts                     getPaperDetail
    ├── saved.ts                      listSaved, savePaper, unsavePaper, markRead
    ├── interests.ts                  listInterests, createInterests, getSettings, upsertSettings
    ├── queries.test.ts               로컬 DB 통합 테스트 (DATABASE_URL 없으면 skip)
    └── index.ts
apps/web/
├── lib/session.ts                    requireUser()
├── lib/link.ts                       sourceUrl(paper)
├── lib/actions/{interests,saved}.ts  'use server'
├── components/
│   ├── icons.tsx
│   ├── ui/{Button,Chip,Switch,Stepper,SegmentedControl}.tsx
│   ├── shell/{Masthead,BottomTabs,TopBar}.tsx
│   ├── paper/{TrackBadge,EvidenceChips,PaperCard,PaperCardView,PaperReadView,TrustDetail}.tsx
│   └── brief/{AudioStrip,ProgressSegments,FollowUpNote}.tsx
├── app/
│   ├── layout.tsx                    next/font 전환 (수정)
│   ├── (app)/layout.tsx              requireUser + 온보딩 게이트 + 셸
│   ├── (app)/page.tsx                /
│   ├── (app)/paper/[id]/page.tsx
│   ├── (app)/paper/[id]/trust/page.tsx
│   ├── (app)/saved/page.tsx
│   ├── (app)/audio/page.tsx
│   ├── onboarding/page.tsx
│   └── page.tsx                      삭제 (→ (app)/page.tsx)
├── proxy.ts                          /api/* 401 (수정)
└── smoke.test.ts                     라우트 스모크
```

---

### Task 1: `PaperSummary` + 매퍼

**Files:**
- Modify: `packages/core/src/paper.ts`
- Create: `packages/db/src/queries/mappers.ts`, `packages/db/src/queries/mappers.test.ts`

**Interfaces:**
- Consumes: core 스키마 `Paper`, `Assessment`, `BriefItem`, `SavedItem`, `UserSettings`, `Interest`; db 테이블 타입.
- Produces:
  - `packages/core`: `PaperSummary` (값 + 타입) = `Paper.omit({ embedding: true })`
  - `packages/db/src/queries/mappers.ts`:
    - `rowToPaper(row): Paper`
    - `rowToPaperSummary(row): PaperSummary`
    - `rowToAssessment(row): Assessment`
    - `rowToBriefItem(row): BriefItem` — `briefId`/`position` 제외, `paperId`·`interestId` 포함
    - `rowToSavedItem(row): SavedItem`
    - `rowToUserSettings(row): UserSettings`
    - `rowToInterest(row): Interest`
  - 각 함수는 core 스키마 `.parse()`의 결과를 반환한다. 파싱 실패는 그대로 throw(쿼리 계층 버그이므로 숨기지 않는다).

- [ ] **Step 1: `PaperSummary`를 core에 추가**

`packages/core/src/paper.ts` 맨 아래에 추가:

```ts
/** 목록 화면용. 1024차원 임베딩을 끌고 오지 않는다 */
export const PaperSummary = Paper.omit({ embedding: true })
export type PaperSummary = z.infer<typeof PaperSummary>
```

- [ ] **Step 2: 실패하는 매퍼 테스트 작성**

`packages/db/src/queries/mappers.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import {
  rowToAssessment,
  rowToBriefItem,
  rowToPaper,
  rowToPaperSummary,
  rowToSavedItem,
  rowToUserSettings,
} from './mappers'

const paperRow = {
  id: '3f1c1a6e-1b7e-4c2a-9c1d-0a1b2c3d4e5f',
  doi: '10.1000/x',
  arxivId: null,
  title: '제목',
  authors: [{ name: '홍길동' }],
  abstract: '초록',
  publishedAt: new Date('2026-09-19T00:00:00Z'),
  source: 'arxiv' as const,
  venue: { name: 'NeurIPS 2026', kind: 'conference' as const },
  pdfUrl: 'https://example.org/a.pdf',
  codeUrl: null,
  openAccess: true,
  embedding: Array.from({ length: 1024 }, () => 0),
  mergedInto: null,
  createdAt: new Date('2026-09-19T00:00:00Z'),
}

describe('rowToPaper', () => {
  it('행을 도메인 Paper로 바꾸고 createdAt 같은 DB 전용 컬럼을 떨어뜨린다', () => {
    const paper = rowToPaper(paperRow)
    expect(paper.title).toBe('제목')
    expect(paper.publishedAt).toBeInstanceOf(Date)
    expect('createdAt' in paper).toBe(false)
  })
  it('임베딩이 null인 행도 받는다', () => {
    expect(rowToPaper({ ...paperRow, embedding: null }).embedding).toBeNull()
  })
})

describe('rowToPaperSummary', () => {
  it('임베딩을 뺀 객체를 돌려준다', () => {
    const summary = rowToPaperSummary({ ...paperRow, embedding: undefined })
    expect('embedding' in summary).toBe(false)
    expect(summary.title).toBe('제목')
  })
})

describe('rowToUserSettings', () => {
  it('pg time의 초 단위를 잘라 HH:mm으로 만든다', () => {
    const settings = rowToUserSettings({
      userId: 'u1',
      departureTime: '08:10:00',
      papersPerDay: 4,
      includePreprints: true,
      updatedAt: new Date(),
    })
    expect(settings.departureTime).toBe('08:10')
  })
  it('이미 HH:mm이면 그대로 둔다', () => {
    const settings = rowToUserSettings({
      userId: 'u1',
      departureTime: '08:10',
      papersPerDay: 4,
      includePreprints: true,
      updatedAt: new Date(),
    })
    expect(settings.departureTime).toBe('08:10')
  })
})

describe('rowToSavedItem', () => {
  const base = {
    userId: 'u1',
    paperId: paperRow.id,
    savedAt: new Date('2026-09-02T00:00:00Z'),
    readAt: null,
    memo: null,
  }
  it('jsonb의 followUp.at ISO 문자열을 Date로 복원한다', () => {
    const item = rowToSavedItem({
      ...base,
      followUp: { kind: 'accepted' as const, text: 'Sleep에 채택', at: '2026-09-20T00:00:00.000Z' },
    })
    expect(item.followUp?.at).toBeInstanceOf(Date)
    expect(item.followUp?.kind).toBe('accepted')
  })
  it('followUp이 없으면 null이다', () => {
    expect(rowToSavedItem({ ...base, followUp: null }).followUp).toBeNull()
  })
})

describe('rowToAssessment', () => {
  it('stage jsonb와 evidence를 그대로 파싱한다', () => {
    const score = { value: 0.8, reason: '근거' }
    const assessment = rowToAssessment({
      paperId: paperRow.id,
      track: 'verified' as const,
      field: 'cs' as const,
      stage1: { passed: true, retracted: false, predatoryVenue: false, paperMillSignals: [] },
      stage2: { venueTier: 'A*', reviewStatus: 'accepted', reviewScore: null, authorTrackRecord: 0.4 },
      stage3: {
        reproducibility: score, design: score, statistics: score,
        claimVsEvidence: score, limitations: score, preregistered: null, studyDesign: null,
      },
      stage4: { influentialCitations: 0, githubStars: 12, mentions: 0 },
      evidence: [{ stage: 2 as const, verdict: 'pass' as const, text: '동료심사 통과' }],
      caveats: [],
      assessedAt: new Date('2026-09-22T00:00:00Z'),
    })
    expect(assessment.track).toBe('verified')
    expect(assessment.evidence).toHaveLength(1)
  })
})

describe('rowToBriefItem', () => {
  it('briefId와 position을 떨어뜨리고 도메인 BriefItem을 만든다', () => {
    const item = rowToBriefItem({
      briefId: '8d3b6a1e-9a2f-4f0c-8a5e-7b6c5d4e3f2a',
      position: 0,
      paperId: paperRow.id,
      interestId: null,
      oneLine: '한 줄',
      whyItMatters: '왜 중요한가',
      method: '방법',
      results: [{ label: '표본', value: '96명' }],
      limitations: [{ bySource: 'author' as const, text: '한계' }],
      quotes: [{ text: 'quote', locator: 'Abstract' }],
      isSerendipity: false,
    })
    expect(item.position).toBe(0)
    expect('briefId' in item).toBe(false)
    expect(item.interestId).toBeNull()
  })
})
```

- [ ] **Step 3: 실패 확인**

Run: `pnpm --filter @jogan/db test mappers`
Expected: FAIL — `./mappers` 모듈 없음.

- [ ] **Step 4: 매퍼 구현**

`packages/db/src/queries/mappers.ts`:

```ts
import {
  Assessment,
  BriefItem,
  Interest,
  Paper,
  PaperSummary,
  SavedItem,
  UserSettings,
} from '@jogan/core'

/**
 * DB 행 → 도메인 객체. 모든 읽기는 여기를 거친다.
 * core 스키마 .parse()로 끝나므로 DB가 돌려준 모양이 도메인 계약과 어긋나면 즉시 터진다.
 */

export function rowToPaper(row: unknown): Paper {
  return Paper.parse(row)
}

export function rowToPaperSummary(row: unknown): PaperSummary {
  return PaperSummary.parse(row)
}

export function rowToAssessment(row: unknown): Assessment {
  return Assessment.parse(row)
}

export function rowToBriefItem(row: unknown): BriefItem {
  return BriefItem.parse(row)
}

export function rowToSavedItem(row: unknown): SavedItem {
  // followUp.at은 jsonb라 ISO 문자열로 온다. SavedItem의 z.coerce.date()가 Date로 되돌린다.
  return SavedItem.parse(row)
}

export function rowToInterest(row: unknown): Interest {
  return Interest.parse(row)
}

export function rowToUserSettings(row: { departureTime: string } & Record<string, unknown>): UserSettings {
  // pg time은 'HH:mm:ss'로 돌아온다. core는 'HH:mm'만 받는다.
  return UserSettings.parse({ ...row, departureTime: row.departureTime.slice(0, 5) })
}
```

zod는 `.parse()`에서 스키마에 없는 키를 떨어뜨리므로 `createdAt`·`briefId`·`position` 같은 DB 전용 컬럼은 자동으로 빠진다. `BriefItem`은 `position`을 포함하므로 남는다.

- [ ] **Step 5: 통과 확인**

Run: `pnpm --filter @jogan/db test mappers && pnpm --filter @jogan/core test && pnpm typecheck`
Expected: 전부 PASS, tsc 오류 0.

- [ ] **Step 6: 커밋**

```bash
git add packages/core packages/db
git commit -m "core: PaperSummary 추가 / db: 행→도메인 매퍼

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: 쿼리 함수와 통합 테스트

**Files:**
- Create: `packages/db/src/queries/briefs.ts`, `papers.ts`, `saved.ts`, `interests.ts`, `index.ts`, `queries.test.ts`
- Modify: `packages/db/src/index.ts` (queries re-export), `packages/db/src/client.ts`

**Interfaces:**
- Consumes: Task 1의 매퍼, 기존 테이블.
- Produces (전부 `@jogan/db`에서 import 가능):
  ```ts
  type BriefView = {
    brief: { id: string; date: string; issueNumber: number; readMinutes: number
             audioUrl: string | null; audioSeconds: number | null }
    items: { item: BriefItem; paper: PaperSummary; assessment: Assessment | null }[]
    isToday: boolean
  }
  getTodayBrief(userId: string, today: string): Promise<BriefView | null>
  getLatestBrief(userId: string): Promise<BriefView | null>
  countStreak(userId: string, today: string): Promise<number>

  type PaperDetail = { paper: Paper; assessment: Assessment | null
                       briefItem: BriefItem | null; saved: SavedItem | null }
  getPaperDetail(paperId: string, userId: string): Promise<PaperDetail | null>
  getRelatedInBrief(paperId: string, userId: string): Promise<PaperSummary[]>   // 같은 브리핑의 다른 항목 최대 2편

  type SavedView = { item: SavedItem; paper: PaperSummary; assessment: Assessment | null }
  listSaved(userId: string): Promise<SavedView[]>
  savePaper(userId: string, paperId: string): Promise<void>
  unsavePaper(userId: string, paperId: string): Promise<void>
  markRead(userId: string, paperId: string): Promise<void>

  listInterests(userId: string): Promise<Interest[]>
  createInterests(userId: string, labels: string[]): Promise<void>
  getSettings(userId: string): Promise<UserSettings | null>
  upsertSettings(s: UserSettings): Promise<void>
  countByInterest(userId: string, sinceDays: number): Promise<{ interestId: string; label: string; count: number }[]>
  ```
- `todayInSeoul(): string`도 `queries/index.ts`에서 export한다(`new Date().toLocaleDateString('en-CA', { timeZone: 'Asia/Seoul' })`). `seed.ts`의 같은 함수는 여기서 import하도록 바꾼다.

- [ ] **Step 1: client.ts에 dev HMR 캐시와 풀 설정**

`packages/db/src/client.ts` 전체를 교체:

```ts
import { drizzle } from 'drizzle-orm/postgres-js'
import postgres from 'postgres'
import { env } from './env'
import * as schema from './schema'

// next dev의 HMR은 모듈을 다시 평가한다. 캐시하지 않으면 커넥션이 쌓여 "too many clients"가 난다.
const globalForDb = globalThis as unknown as { __joganSql?: ReturnType<typeof postgres> }

// prepare: false — Neon 풀링(pgbouncer) 호환
const sql =
  globalForDb.__joganSql ??
  postgres(env.DATABASE_URL, { prepare: false, max: 10, idle_timeout: 20 })

if (process.env.NODE_ENV !== 'production') globalForDb.__joganSql = sql

export const db = drizzle(sql, { schema })
export type Db = typeof db
```

- [ ] **Step 2: 브리핑 쿼리**

`packages/db/src/queries/briefs.ts`:

```ts
import type { Assessment, BriefItem, PaperSummary } from '@jogan/core'
import { and, desc, eq, lte } from 'drizzle-orm'
import { db } from '../client'
import { assessments, briefItems, briefs, papers } from '../schema'
import { rowToAssessment, rowToBriefItem, rowToPaperSummary } from './mappers'

export type BriefView = {
  brief: {
    id: string
    date: string
    issueNumber: number
    readMinutes: number
    audioUrl: string | null
    audioSeconds: number | null
  }
  items: { item: BriefItem; paper: PaperSummary; assessment: Assessment | null }[]
  /** 오늘자 브리핑인가. false면 화면에 "지난 브리핑" 라벨을 붙인다 */
  isToday: boolean
}

/** 목록에서 임베딩을 제외한 논문 컬럼 */
const paperColumns = {
  id: true, doi: true, arxivId: true, title: true, authors: true, abstract: true,
  publishedAt: true, source: true, venue: true, pdfUrl: true, codeUrl: true,
  openAccess: true, mergedInto: true,
} as const

async function loadBrief(briefRow: typeof briefs.$inferSelect, today: string): Promise<BriefView> {
  const rows = await db
    .select({ item: briefItems, paper: papers, assessment: assessments })
    .from(briefItems)
    .innerJoin(papers, eq(briefItems.paperId, papers.id))
    .leftJoin(assessments, eq(assessments.paperId, papers.id))
    .where(eq(briefItems.briefId, briefRow.id))
    .orderBy(briefItems.position)

  return {
    brief: {
      id: briefRow.id,
      date: briefRow.date,
      issueNumber: briefRow.issueNumber,
      readMinutes: briefRow.readMinutes,
      audioUrl: briefRow.audioUrl,
      audioSeconds: briefRow.audioSeconds,
    },
    items: rows.map((r) => {
      const { embedding: _embedding, createdAt: _createdAt, ...paper } = r.paper
      return {
        item: rowToBriefItem(r.item),
        paper: rowToPaperSummary(paper),
        assessment: r.assessment ? rowToAssessment(r.assessment) : null,
      }
    }),
    isToday: briefRow.date === today,
  }
}

export async function getTodayBrief(userId: string, today: string): Promise<BriefView | null> {
  const row = await db.query.briefs.findFirst({
    where: and(eq(briefs.userId, userId), eq(briefs.date, today)),
  })
  return row ? loadBrief(row, today) : null
}

/** 오늘자가 없을 때 쓴다. 빈 브리핑을 보여주지 않는다 (PRD §5) */
export async function getLatestBrief(userId: string): Promise<BriefView | null> {
  const row = await db.query.briefs.findFirst({
    where: eq(briefs.userId, userId),
    orderBy: [desc(briefs.date)],
  })
  return row ? loadBrief(row, row.date) : null
}

/** 오늘(또는 어제)부터 하루도 빠지지 않고 이어진 브리핑 일수 */
export async function countStreak(userId: string, today: string): Promise<number> {
  const rows = await db
    .select({ date: briefs.date })
    .from(briefs)
    .where(and(eq(briefs.userId, userId), lte(briefs.date, today)))
    .orderBy(desc(briefs.date))

  const dates = rows.map((r) => r.date)
  if (dates.length === 0) return 0

  const dayMs = 86_400_000
  const todayMs = Date.parse(`${today}T00:00:00Z`)
  const firstMs = Date.parse(`${dates[0]}T00:00:00Z`)
  // 오늘 것이 아직 없으면 어제까지 이어진 기록을 센다
  if (todayMs - firstMs > dayMs) return 0

  let streak = 1
  for (let i = 1; i < dates.length; i++) {
    const prev = Date.parse(`${dates[i - 1]}T00:00:00Z`)
    const cur = Date.parse(`${dates[i]}T00:00:00Z`)
    if (prev - cur !== dayMs) break
    streak++
  }
  return streak
}
```

`paperColumns`는 관계형 쿼리에서 쓰려고 둔 것인데 위 구현은 `select`로 가져와 구조분해로 임베딩을 버린다. 둘 중 하나로 통일하라 — **`select` + 구조분해를 쓰고 `paperColumns` 상수는 지운다.**

- [ ] **Step 3: 논문 · 저장 · 관심사 쿼리**

`packages/db/src/queries/papers.ts`:

```ts
import type { Assessment, BriefItem, Paper, PaperSummary, SavedItem } from '@jogan/core'
import { and, eq, ne } from 'drizzle-orm'
import { db } from '../client'
import { assessments, briefItems, briefs, papers, savedItems } from '../schema'
import { rowToAssessment, rowToBriefItem, rowToPaper, rowToPaperSummary, rowToSavedItem } from './mappers'

export type PaperDetail = {
  paper: Paper
  assessment: Assessment | null
  /** 이 사용자의 브리핑에 실렸다면 그 항목 (요약 텍스트가 여기 있다) */
  briefItem: BriefItem | null
  saved: SavedItem | null
}

export async function getPaperDetail(paperId: string, userId: string): Promise<PaperDetail | null> {
  const paperRow = await db.query.papers.findFirst({ where: eq(papers.id, paperId) })
  if (!paperRow) return null

  const [assessmentRow, itemRow, savedRow] = await Promise.all([
    db.query.assessments.findFirst({ where: eq(assessments.paperId, paperId) }),
    db
      .select({ item: briefItems })
      .from(briefItems)
      .innerJoin(briefs, eq(briefItems.briefId, briefs.id))
      .where(and(eq(briefItems.paperId, paperId), eq(briefs.userId, userId)))
      .limit(1),
    db.query.savedItems.findFirst({
      where: and(eq(savedItems.userId, userId), eq(savedItems.paperId, paperId)),
    }),
  ])

  const { createdAt: _createdAt, ...paper } = paperRow
  return {
    paper: rowToPaper(paper),
    assessment: assessmentRow ? rowToAssessment(assessmentRow) : null,
    briefItem: itemRow[0] ? rowToBriefItem(itemRow[0].item) : null,
    saved: savedRow ? rowToSavedItem(savedRow) : null,
  }
}

/**
 * "함께 읽으면 좋은 논문" — 추천 로직이 아직 없다.
 * 같은 브리핑의 다른 항목에서 최대 2편을 가져온다.
 */
export async function getRelatedInBrief(paperId: string, userId: string): Promise<PaperSummary[]> {
  const rows = await db
    .select({ paper: papers })
    .from(briefItems)
    .innerJoin(briefs, eq(briefItems.briefId, briefs.id))
    .innerJoin(papers, eq(briefItems.paperId, papers.id))
    .where(
      and(
        eq(briefs.userId, userId),
        ne(briefItems.paperId, paperId),
        eq(
          briefItems.briefId,
          db
            .select({ id: briefItems.briefId })
            .from(briefItems)
            .where(eq(briefItems.paperId, paperId))
            .limit(1),
        ),
      ),
    )
    .limit(2)

  return rows.map((r) => {
    const { embedding: _embedding, createdAt: _createdAt, ...paper } = r.paper
    return rowToPaperSummary(paper)
  })
}
```

서브쿼리가 타입 문제를 일으키면 두 번의 쿼리로 나눠라(먼저 `briefId`를 찾고, 그 다음 항목을 가져온다). 동작이 같으면 어느 쪽이든 좋다.

`packages/db/src/queries/saved.ts`:

```ts
import type { Assessment, PaperSummary, SavedItem } from '@jogan/core'
import { and, desc, eq } from 'drizzle-orm'
import { db } from '../client'
import { assessments, papers, savedItems } from '../schema'
import { rowToAssessment, rowToPaperSummary, rowToSavedItem } from './mappers'

export type SavedView = { item: SavedItem; paper: PaperSummary; assessment: Assessment | null }

export async function listSaved(userId: string): Promise<SavedView[]> {
  const rows = await db
    .select({ item: savedItems, paper: papers, assessment: assessments })
    .from(savedItems)
    .innerJoin(papers, eq(savedItems.paperId, papers.id))
    .leftJoin(assessments, eq(assessments.paperId, papers.id))
    .where(eq(savedItems.userId, userId))
    .orderBy(desc(savedItems.savedAt))

  return rows.map((r) => {
    const { embedding: _embedding, createdAt: _createdAt, ...paper } = r.paper
    return {
      item: rowToSavedItem(r.item),
      paper: rowToPaperSummary(paper),
      assessment: r.assessment ? rowToAssessment(r.assessment) : null,
    }
  })
}

export async function savePaper(userId: string, paperId: string): Promise<void> {
  await db.insert(savedItems).values({ userId, paperId }).onConflictDoNothing()
}

export async function unsavePaper(userId: string, paperId: string): Promise<void> {
  await db
    .delete(savedItems)
    .where(and(eq(savedItems.userId, userId), eq(savedItems.paperId, paperId)))
}

export async function markRead(userId: string, paperId: string): Promise<void> {
  await db
    .update(savedItems)
    .set({ readAt: new Date() })
    .where(and(eq(savedItems.userId, userId), eq(savedItems.paperId, paperId)))
}
```

`packages/db/src/queries/interests.ts`:

```ts
import type { Interest, UserSettings } from '@jogan/core'
import { and, eq, gte, sql } from 'drizzle-orm'
import { db } from '../client'
import { briefItems, briefs, interests, userSettings } from '../schema'
import { rowToInterest, rowToUserSettings } from './mappers'

export async function listInterests(userId: string): Promise<Interest[]> {
  const rows = await db.query.interests.findMany({ where: eq(interests.userId, userId) })
  return rows.map(({ createdAt: _createdAt, ...row }) => rowToInterest(row))
}

/** 온보딩 제출. 임베딩은 파이프라인이 나중에 채운다 */
export async function createInterests(userId: string, labels: string[]): Promise<void> {
  if (labels.length === 0) return
  await db
    .insert(interests)
    .values(labels.map((label) => ({ userId, label, embedding: null, seedPaperIds: [] })))
}

export async function getSettings(userId: string): Promise<UserSettings | null> {
  const row = await db.query.userSettings.findFirst({ where: eq(userSettings.userId, userId) })
  return row ? rowToUserSettings(row) : null
}

export async function upsertSettings(s: UserSettings): Promise<void> {
  await db
    .insert(userSettings)
    .values({ ...s, updatedAt: new Date() })
    .onConflictDoUpdate({
      target: userSettings.userId,
      set: {
        departureTime: s.departureTime,
        papersPerDay: s.papersPerDay,
        includePreprints: s.includePreprints,
        updatedAt: new Date(),
      },
    })
}

/** 웹 우 레일 "이번 주 관심사 흐름" */
export async function countByInterest(
  userId: string,
  sinceDays: number,
): Promise<{ interestId: string; label: string; count: number }[]> {
  const since = new Date(Date.now() - sinceDays * 86_400_000).toISOString().slice(0, 10)
  const rows = await db
    .select({
      interestId: interests.id,
      label: interests.label,
      count: sql<number>`count(*)::int`,
    })
    .from(briefItems)
    .innerJoin(briefs, eq(briefItems.briefId, briefs.id))
    .innerJoin(interests, eq(briefItems.interestId, interests.id))
    .where(and(eq(briefs.userId, userId), gte(briefs.date, since)))
    .groupBy(interests.id, interests.label)
  return rows
}
```

`packages/db/src/queries/index.ts`:

```ts
export * from './mappers'
export * from './briefs'
export * from './papers'
export * from './saved'
export * from './interests'

/** KST 기준 오늘 (YYYY-MM-DD). 배치가 새벽 3시 KST에 돌므로 날짜 기준도 KST다 */
export function todayInSeoul(): string {
  return new Date().toLocaleDateString('en-CA', { timeZone: 'Asia/Seoul' })
}
```

`packages/db/src/index.ts`에 한 줄 추가: `export * from './queries'`

`packages/db/src/seed.ts`의 로컬 `todayInSeoul` 정의를 지우고 `./queries`에서 import한다.

- [ ] **Step 4: 통합 테스트**

`packages/db/src/queries/queries.test.ts`:

```ts
import { afterAll, describe, expect, it } from 'vitest'

const hasDb = Boolean(process.env.DATABASE_URL)

// DATABASE_URL이 없는 환경(CI 등)에서는 건너뛴다. 로컬에서는 `pnpm db:seed` 후 돌린다.
describe.skipIf(!hasDb)('queries (로컬 DB · 시드 데이터 기준)', () => {
  it('오늘 브리핑을 항목·논문·평가와 함께 가져온다', async () => {
    const { db, getTodayBrief, todayInSeoul, users } = await import('../index')
    const user = await db.query.users.findFirst()
    expect(user).toBeDefined()
    if (!user) return

    const view = await getTodayBrief(user.id, todayInSeoul())
    expect(view).not.toBeNull()
    expect(view?.items).toHaveLength(4)
    expect(view?.isToday).toBe(true)
    // 목록은 임베딩을 끌고 오지 않는다
    expect(view?.items[0] && 'embedding' in view.items[0].paper).toBe(false)
    // 순서는 position대로
    expect(view?.items.map((i) => i.item.position)).toEqual([0, 1, 2, 3])
    // 곁가지가 정확히 1편
    expect(view?.items.filter((i) => i.item.isSerendipity)).toHaveLength(1)
  })

  it('논문 상세에 평가와 브리핑 항목이 함께 온다', async () => {
    const { db, getPaperDetail, getTodayBrief, todayInSeoul } = await import('../index')
    const user = await db.query.users.findFirst()
    if (!user) return
    const view = await getTodayBrief(user.id, todayInSeoul())
    const first = view?.items[0]
    expect(first).toBeDefined()
    if (!first) return

    const detail = await getPaperDetail(first.paper.id, user.id)
    expect(detail?.paper.id).toBe(first.paper.id)
    expect(detail?.assessment).not.toBeNull()
    expect(detail?.briefItem?.oneLine).toBe(first.item.oneLine)
  })

  it('저장함의 후속 소식 at이 Date로 복원된다', async () => {
    const { db, listSaved } = await import('../index')
    const user = await db.query.users.findFirst()
    if (!user) return
    const saved = await listSaved(user.id)
    expect(saved.length).toBeGreaterThan(0)
    const withFollowUp = saved.find((s) => s.item.followUp)
    expect(withFollowUp?.item.followUp?.at).toBeInstanceOf(Date)
  })

  it('설정의 출발 시각이 HH:mm으로 온다', async () => {
    const { db, getSettings } = await import('../index')
    const user = await db.query.users.findFirst()
    if (!user) return
    const settings = await getSettings(user.id)
    expect(settings?.departureTime).toMatch(/^([01]\d|2[0-3]):[0-5]\d$/)
  })

  it('연속 기록이 0 이상이다', async () => {
    const { db, countStreak, todayInSeoul } = await import('../index')
    const user = await db.query.users.findFirst()
    if (!user) return
    expect(await countStreak(user.id, todayInSeoul())).toBeGreaterThanOrEqual(1)
  })
})

afterAll(async () => {
  if (!hasDb) return
  // 커넥션을 닫지 않으면 vitest가 종료되지 않는다
  const { db } = await import('../index')
  await (db as unknown as { $client: { end: () => Promise<void> } }).$client.end()
})
```

`$client`로 커넥션을 닫는 방법이 drizzle 0.45에서 다르면, `postgres` 인스턴스를 `client.ts`에서 `export const sql`로 내보내고 그것을 `end()` 한다.

- [ ] **Step 5: 검증**

```bash
docker compose up -d
pnpm db:seed
pnpm --filter @jogan/db test
pnpm typecheck
```
Expected: 매퍼 테스트 + 시드 테스트 + 통합 테스트 전부 PASS. tsc 오류 0.

- [ ] **Step 6: 커밋**

```bash
git add packages/db
git commit -m "db: 쿼리 계층 (브리핑·논문·저장·관심사) 와 통합 테스트

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: 웹 기반 — 폰트, 세션 헬퍼, 프록시 401, 링크 유틸

**Files:**
- Modify: `apps/web/app/layout.tsx`, `apps/web/proxy.ts`
- Create: `apps/web/lib/session.ts`, `apps/web/lib/link.ts`

**Interfaces:**
- Produces:
  - `@/lib/session`: `requireUser(): Promise<{ id: string; email: string; name: string | null; image: string | null }>` — 세션이 없으면 `redirect('/login')`
  - `@/lib/link`: `sourceUrl(paper: { doi: string | null; arxivId: string | null; pdfUrl: string | null }): string | null`

- [ ] **Step 1: `next/font`로 전환**

`apps/web/app/layout.tsx`에서 `<link>` 3줄과 `FONTS_URL` 상수를 지우고 폰트를 `next/font/google`로 바꾼다:

```tsx
import { IBM_Plex_Sans_KR, Nanum_Myeongjo } from 'next/font/google'
import type { Metadata, Viewport } from 'next'
import type { ReactNode } from 'react'
import './globals.css'

const display = Nanum_Myeongjo({
  subsets: ['latin'],
  weight: ['400', '700', '800'],
  variable: '--font-display',
  display: 'swap',
})

const body = IBM_Plex_Sans_KR({
  subsets: ['latin'],
  weight: ['400', '500', '600', '700'],
  variable: '--font-body',
  display: 'swap',
})

export const metadata: Metadata = {
  title: '조간 논문',
  description: '매일 아침, 믿을 만한 논문 4편',
  appleWebApp: { capable: true, title: '조간 논문', statusBarStyle: 'default' },
}

export const viewport: Viewport = {
  themeColor: '#F7F4ED',
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html lang="ko" className={`${display.variable} ${body.variable}`}>
      <body className="min-h-dvh">{children}</body>
    </html>
  )
}
```

`apps/web/tailwind.config.ts`의 `fontFamily`를 CSS 변수로 바꾼다:

```ts
      fontFamily: {
        display: ['var(--font-display)', 'serif'],
        body: ['var(--font-body)', 'sans-serif'],
      },
```

- [ ] **Step 2: 세션 헬퍼와 링크 유틸**

`apps/web/lib/session.ts`:

```ts
import { redirect } from 'next/navigation'
import { auth } from '@/auth'

export type SessionUser = {
  id: string
  email: string
  name: string | null
  image: string | null
}

/** 로그인이 필요한 모든 페이지의 첫 줄. 세션이 없으면 /login으로 보낸다 */
export async function requireUser(): Promise<SessionUser> {
  const session = await auth()
  const user = session?.user
  if (!user?.id || !user.email) redirect('/login')
  return {
    id: user.id,
    email: user.email,
    name: user.name ?? null,
    image: user.image ?? null,
  }
}
```

`session.user.id`가 타입에 없으면 `auth.ts`의 `callbacks.session`에서 `session.user.id = user.id`를 채우고 `next-auth.d.ts`로 모듈 보강을 한다 — DB 세션 전략에서는 `user`가 어댑터 사용자이므로 `id`가 있다.

`apps/web/lib/link.ts`:

```ts
/** 원문 링크. DOI > arXiv > PDF 순 (CLAUDE.md 규칙 4) */
export function sourceUrl(paper: {
  doi: string | null
  arxivId: string | null
  pdfUrl: string | null
}): string | null {
  if (paper.doi) return `https://doi.org/${paper.doi}`
  if (paper.arxivId) return `https://arxiv.org/abs/${paper.arxivId}`
  return paper.pdfUrl
}
```

- [ ] **Step 3: 프록시가 `/api/*`에는 401을 준다**

`apps/web/proxy.ts`의 리다이렉트 부분을 바꾼다 — 인증 안 된 API 요청에 HTML 리다이렉트를 주지 않는다:

```ts
  if (pathname.startsWith('/api/')) {
    return NextResponse.json({ error: 'unauthorized' }, { status: 401 })
  }

  const url = req.nextUrl.clone()
  url.pathname = '/login'
  url.search = ''
  return NextResponse.redirect(url)
```

- [ ] **Step 4: 검증**

```bash
pnpm --filter @jogan/web typecheck
pnpm dev &
sleep 10
curl -s -o /dev/null -w "%{http_code} " http://localhost:3000/          # 307
curl -s -o /dev/null -w "%{http_code} " http://localhost:3000/api/hello # 401
curl -s http://localhost:3000/login | grep -o 'Google로 계속하기'
kill %1
```
Expected: 307, 401, `Google로 계속하기`. 폰트가 `<link>` 없이 자체 호스팅으로 바뀌었는지 `curl -s http://localhost:3000/login | grep -c 'fonts.googleapis.com'` 가 0인지 확인.

- [ ] **Step 5: 커밋**

```bash
git add apps/web
git commit -m "web: next/font 전환, requireUser·sourceUrl 유틸, API 401

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: 디자인 프리미티브 — 아이콘과 ui 컴포넌트

**Files:**
- Create: `apps/web/components/icons.tsx`, `apps/web/components/ui/Button.tsx`, `Chip.tsx`, `Switch.tsx`, `Stepper.tsx`, `SegmentedControl.tsx`

**Interfaces:**
- Produces:
  - `icons.tsx`: `CheckCircleIcon`, `WarningTriangleIcon`, `ShieldIcon`, `BookmarkIcon`, `ExternalLinkIcon`, `CloseIcon`, `ChevronLeftIcon`, `ChevronDownIcon`, `PlayIcon`, `PauseIcon`, `SkipBackIcon`, `SkipForwardIcon`, `PlusIcon`, `SearchIcon`, `HomeIcon`, `InterestIcon`. 전부 `(props: { className?: string }) => JSX.Element`, `viewBox="0 0 24 24"`, `fill="none"`, `stroke="currentColor"`, `strokeWidth={1.5}`, `strokeLinecap="round"`, `strokeLinejoin="round"`, `aria-hidden="true"`.
  - `Button.tsx`: `<Button variant="primary" | "secondary" | "ghost" size="lg" | "md">` — `lg`는 h-[54px], `md`는 h-11(44px). 실제 `<button>`이며 `...props`를 넘긴다.
  - `Chip.tsx`: `<Chip tone="neutral" | "verified" | "caution">` — 표시 전용 `<span>`, 10.5px/500.
  - `Switch.tsx`: `'use client'`. 트랙 50×28, 노브 22px, 버튼 영역 52×44. `<button role="switch" aria-checked>`.
  - `Stepper.tsx`: `'use client'`. −/+ 각 44×44, `aria-label` 필수, 값은 `tabular-nums`.
  - `SegmentedControl.tsx`: `'use client'`. `<div role="tablist">` 안에 실제 `<button role="tab">`. 높이 44px.

- [ ] **Step 1: 아이콘**

`apps/web/components/icons.tsx` — 각 아이콘은 같은 껍데기를 쓴다:

```tsx
import type { SVGProps } from 'react'

type IconProps = { className?: string } & Omit<SVGProps<SVGSVGElement>, 'className'>

function Icon({ children, ...props }: IconProps & { children: React.ReactNode }) {
  return (
    <svg
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth={1.5}
      strokeLinecap="round"
      strokeLinejoin="round"
      aria-hidden="true"
      {...props}
    >
      {children}
    </svg>
  )
}

export function CheckCircleIcon(props: IconProps) {
  return (
    <Icon {...props}>
      <circle cx="12" cy="12" r="9" />
      <path d="m8.5 12.2 2.4 2.4 4.6-4.9" />
    </Icon>
  )
}

export function WarningTriangleIcon(props: IconProps) {
  return (
    <Icon {...props}>
      <path d="M12 4.6 2.9 19.4h18.2L12 4.6Z" />
      <path d="M12 10v4" />
      <path d="M12 17h.01" />
    </Icon>
  )
}
```

나머지 아이콘도 같은 방식으로, 24×24 그리드에 맞춰 직접 그린다. 외부 아이콘 라이브러리를 설치하지 않는다.

- [ ] **Step 2: ui 컴포넌트**

`apps/web/components/ui/Button.tsx`:

```tsx
import type { ButtonHTMLAttributes } from 'react'

type Props = ButtonHTMLAttributes<HTMLButtonElement> & {
  variant?: 'primary' | 'secondary' | 'ghost'
  size?: 'lg' | 'md'
}

const VARIANT = {
  primary: 'bg-ink text-paper hover:bg-ink-body',
  secondary: 'bg-surface text-ink-body border border-line-strong hover:bg-paper-raised',
  ghost: 'text-ink-dim hover:bg-paper-raised',
} as const

const SIZE = { lg: 'h-[54px] rounded-xl text-[15px]', md: 'h-11 rounded-[10px] text-sm' } as const

export function Button({ variant = 'primary', size = 'md', className = '', ...props }: Props) {
  return (
    <button
      className={`inline-flex items-center justify-center px-4 font-medium transition-colors disabled:opacity-50 ${VARIANT[variant]} ${SIZE[size]} ${className}`}
      {...props}
    />
  )
}
```

`Chip.tsx`, `Switch.tsx`, `Stepper.tsx`, `SegmentedControl.tsx`도 위 인터페이스대로 만든다. 색·크기는 `docs/DESIGN.md` §1~§3의 값을 토큰 클래스로 옮긴다. 상태가 있는 세 개(`Switch`, `Stepper`, `SegmentedControl`)는 파일 첫 줄에 `'use client'`를 쓰고, 값과 `onChange`를 props로 받는 제어 컴포넌트로 만든다.

- [ ] **Step 3: 검증**

Run: `pnpm --filter @jogan/web typecheck`
Expected: 오류 0. (렌더 확인은 Task 6에서 화면에 붙일 때 한다.)

- [ ] **Step 4: 커밋**

```bash
git add apps/web/components
git commit -m "web: 인라인 SVG 아이콘과 ui 프리미티브

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: 셸 — `(app)` 레이아웃, 하단 탭, 상단바

**Files:**
- Create: `apps/web/components/shell/Masthead.tsx`, `BottomTabs.tsx`, `TopBar.tsx`, `apps/web/app/(app)/layout.tsx`
- Delete: `apps/web/app/page.tsx` (Task 6에서 `(app)/page.tsx`로 대체 — 이 태스크에서는 임시로 `(app)/page.tsx`에 플레이스홀더를 옮긴다)

**Interfaces:**
- Consumes: `requireUser()`, `listInterests()`.
- Produces:
  - `Masthead`: `{ date: string; issueNumber: number | null }` — 제호 `조간 논문` display 27px(폰)/22px(웹) + 날짜·호수 우측 정렬 + 아래 2px `ink` 실선.
  - `BottomTabs`: `'use client'`(현재 경로 강조에 `usePathname` 필요). 3개 탭(브리핑 `/`, 저장함 `/saved`, 관심사 `/onboarding`), 높이 56px + 하단 여백 10px, 각 탭은 `<Link>`이고 터치 영역 44px 이상.
  - `TopBar`: 웹 전용(`hidden tablet:flex`), 높이 66px, 제호 · 날짜/호수 · 검색 입력(380px, `disabled` + placeholder "아카이브 검색 — 준비 중") · `아카이브` · `저장함 N` · 아바타 32px.
  - `(app)/layout.tsx`: `requireUser()` → 관심사 0개면 `redirect('/onboarding')` → 폰은 하단 탭, 웹은 상단바.

- [ ] **Step 1: 셸 컴포넌트**

`Masthead`, `BottomTabs`, `TopBar`를 위 인터페이스대로 만든다. `docs/DESIGN.md` §6의 폰 `/` 제호 영역과 웹 `/` 상단바 명세를 그대로 따른다. 하단 탭 아이콘은 Task 4의 `HomeIcon`/`BookmarkIcon`/`InterestIcon`, 각 버튼에 보이는 라벨이 있으므로 `aria-label`은 생략 가능하되 현재 탭에 `aria-current="page"`를 준다.

- [ ] **Step 2: `(app)` 레이아웃**

`apps/web/app/(app)/layout.tsx`:

```tsx
import { listInterests, todayInSeoul } from '@jogan/db'
import { redirect } from 'next/navigation'
import type { ReactNode } from 'react'
import { BottomTabs } from '@/components/shell/BottomTabs'
import { TopBar } from '@/components/shell/TopBar'
import { requireUser } from '@/lib/session'

export default async function AppLayout({ children }: { children: ReactNode }) {
  const user = await requireUser()
  const interests = await listInterests(user.id)
  if (interests.length === 0) redirect('/onboarding')

  return (
    <div className="min-h-dvh bg-paper">
      <TopBar user={user} today={todayInSeoul()} />
      <div className="pb-[66px] tablet:pb-0">{children}</div>
      <BottomTabs />
    </div>
  )
}
```

`BottomTabs`는 자체적으로 `tablet:hidden`, `TopBar`는 `hidden tablet:flex`를 갖는다.

- [ ] **Step 3: 기존 홈을 `(app)`으로 옮긴다**

`apps/web/app/page.tsx`를 지우고 같은 내용을 `apps/web/app/(app)/page.tsx`로 옮긴다(로그아웃 폼 포함, 내용은 Task 6에서 교체된다). `requireUser()`를 쓰도록 바꾸고 `auth()` 직접 호출을 지운다.

- [ ] **Step 4: 검증**

```bash
pnpm --filter @jogan/web typecheck
pnpm db:seed && pnpm dev &
sleep 10
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/   # 307 (미로그인)
kill %1
```
로그인 상태 확인이 필요하면 브라우저로 직접 본다. 시드 사용자는 관심사가 3개이므로 온보딩으로 튕기지 않아야 한다.

- [ ] **Step 5: 커밋**

```bash
git add apps/web
git commit -m "web: (app) 셸 — 하단 탭, 상단바, 제호, 온보딩 게이트

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 6: `/` — 오늘의 브리핑 (폰 + 웹)

**Files:**
- Create: `apps/web/components/paper/TrackBadge.tsx`, `EvidenceChips.tsx`, `PaperCard.tsx`, `apps/web/components/brief/AudioStrip.tsx`
- Modify: `apps/web/app/(app)/page.tsx`

**Interfaces:**
- Consumes: `getTodayBrief`, `getLatestBrief`, `countStreak`, `countByInterest`, `listInterests`, `todayInSeoul`, `sourceUrl`.
- Produces:
  - `TrackBadge`: `{ track: 'verified' | 'notable' }` — `검증됨`(체크 원, `verified`/`verified-bg`) 또는 `심사 전`(경고 삼각형, `caution`/`caution-bg`). pill.
  - `EvidenceChips`: `{ evidence: Evidence[]; max?: number }` — `verdict==='caution'`이면 `caution` 톤, 아니면 `neutral`. `max` 기본 3(폰), 웹에서 4를 넘긴다.
  - `PaperCard`: `{ item: BriefItem; paper: PaperSummary; assessment: Assessment | null; layout: 'phone' | 'web' }` — 카드 전체가 `/paper/[id]`로 가는 `<a>`(폰) / 웹은 좌측 내용 + 우측 132px 액션 칼럼.
  - `AudioStrip`: `{ seconds: number | null }` — `ink` 배경 다크 바, 원형 재생 버튼 38px, `/audio`로 가는 `<a>`.

- [ ] **Step 1: 페이지 구현**

`apps/web/app/(app)/page.tsx`는 이렇게 데이터를 모은다:

```tsx
import { countByInterest, countStreak, getLatestBrief, getTodayBrief, todayInSeoul } from '@jogan/db'
import { requireUser } from '@/lib/session'

export default async function BriefingPage() {
  const user = await requireUser()
  const today = todayInSeoul()
  const view = (await getTodayBrief(user.id, today)) ?? (await getLatestBrief(user.id))
  const [streak, byInterest] = await Promise.all([
    countStreak(user.id, today),
    countByInterest(user.id, 7),
  ])
  // view가 null이면 "내일 아침 첫 브리핑이 도착합니다" 빈 상태
  // view.isToday가 false면 "9월 22일자 · 오늘 브리핑은 새벽에 준비됩니다" 라벨
  ...
}
```

레이아웃:
- **폰(`<720`)** — 제호 → "관심사 N개에서 오늘 **M편**을 골랐습니다 · 읽기 K분" → `AudioStrip` → 카드 리스트(12~14px 간격) → (하단 탭은 레이아웃이 그림)
- **웹(`tablet:`)** — 3단 `grid`: 좌 244 / 중앙 flex / 우 300, 간격 28. 좌 레일은 관심사 목록(도트 + 이름 + 편수) + `관심사 추가` 점선 버튼 + 걸러내기 체크박스 3개(`동료심사 통과만`, `코드 공개만`, `한국 기관 참여` — 이번엔 표시만, `disabled`) + 연속 기록 display 30px. 중앙은 display 30px "오늘의 브리핑" + "N편을 걸러 M편을 남겼습니다" + 가로형 카드. 우 레일은 후속 소식 / 이번 주 관심사 흐름(`byInterest`) / 오늘의 곁가지(`isSerendipity` 항목) / "새벽 3시에 수집·평가를 마쳤습니다".

"N편을 걸러"의 N은 아직 데이터가 없다 — `assessments` 총 개수를 쓰지 말고 문구를 "오늘 M편을 골랐습니다"로만 쓴다. 가짜 숫자를 만들지 않는다.

- [ ] **Step 2: 검증**

```bash
pnpm --filter @jogan/web typecheck
pnpm db:seed && pnpm dev
```
브라우저에서 로그인 후 `/`를 폰 폭(390)과 웹 폭(1440)으로 본다. 확인: 카드 4장, 세 번째 논문이 `심사 전` 배지 + `caution` 테두리, 근거 칩이 목록에 보이고 부정 신호는 `caution` 색, 곁가지 1편, 배지에 숫자 없음.

- [ ] **Step 3: 커밋**

```bash
git add apps/web
git commit -m "web: / 오늘의 브리핑 (폰 1칼럼 · 웹 3단)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 7: `/paper/[id]` — 카드 상세(폰)와 정독(웹)

**Files:**
- Create: `apps/web/components/paper/PaperCardView.tsx`, `PaperReadView.tsx`, `apps/web/components/brief/ProgressSegments.tsx`, `apps/web/lib/actions/saved.ts`
- Create: `apps/web/app/(app)/paper/[id]/page.tsx`

**Interfaces:**
- Consumes: `getPaperDetail`, `getRelatedInBrief`, `getTodayBrief`(진행 세그먼트의 전체 편수·현재 위치), `sourceUrl`.
- Produces:
  - `saved.ts` 서버 액션: `toggleSave(paperId: string): Promise<void>`, `markPaperRead(paperId: string): Promise<void>` — 둘 다 `'use server'`, 내부에서 `requireUser()`로 사용자를 얻고 `revalidatePath`를 호출한다. **`userId`를 클라이언트에서 받지 않는다.**
  - `PaperCardView`: `{ detail: PaperDetail; position: number; total: number }` — 폰. 진행 세그먼트 → 배지 → display 24px 제목 → 저자·소속·날짜 → `왜 중요한가` → `방법과 결과`(`paper` 배경 박스, 라벨/값) → `한계`(`저자 인정`/`AI 관찰` 구분) → 하단 액션 바(저장 / 신뢰도 근거 / 원문 열기 48×48).
  - `PaperReadView`: `{ detail: PaperDetail; related: PaperSummary[] }` — 웹 3단(좌 236 / 중앙 776 / 우 300). 중앙은 배지 → display 33px 제목 → 저자 → `핵심 3줄`(`<ol>`) → 섹션별 한국어 요약(= `whyItMatters`, `method`) → 영문 원문 인용 블록(`paper-raised` 배경, 좌측 3px inset box-shadow, display italic, 아래 `원문 §… · 클릭하면 PDF의 해당 위치로 이동`) → 결과 표(실제 `<table>` + `<caption>`, 헤더 아래 1.5px `ink` 실선). 우 레일은 신뢰도 근거 요약 4행 + `판정 근거 전체 보기`(→ `/paper/[id]/trust`) + 내 메모(`<label>` + `<textarea>`, 저장 버튼은 이번엔 `disabled`) + 이 논문의 용어(`verified-surface`, 시드 고정값).

- [ ] **Step 1: 페이지와 두 뷰**

`page.tsx`는 데이터만 모으고 두 뷰를 CSS로 고른다:

```tsx
export default async function PaperPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params
  const user = await requireUser()
  const detail = await getPaperDetail(id, user.id)
  if (!detail) notFound()
  const [related, brief] = await Promise.all([
    getRelatedInBrief(id, user.id),
    getTodayBrief(user.id, todayInSeoul()),
  ])
  const position = brief?.items.findIndex((i) => i.paper.id === id) ?? -1

  return (
    <>
      <div className="tablet:hidden">
        <PaperCardView detail={detail} position={position} total={brief?.items.length ?? 0} />
      </div>
      <div className="hidden tablet:block">
        <PaperReadView detail={detail} related={related} />
      </div>
    </>
  )
}
```

Next 16에서 `params`는 Promise다 — `await` 한다.

`핵심 3줄`은 `oneLine` + `whyItMatters`를 쪼개 만들지 말고, `oneLine` 1줄 + `results` 상위 2개를 문장으로 쓴다. 없는 데이터를 지어내지 않는다.

"함께 읽으면 좋은 논문"의 관계 문구는 `related` 각 논문에 대해 고정 문구 하나(`같은 날 브리핑에 함께 실린 논문`)를 쓴다. 가짜 관련도 계산을 하지 않는다.

- [ ] **Step 2: 검증**

```bash
pnpm --filter @jogan/web typecheck
pnpm dev
```
브라우저에서 `/`의 카드를 눌러 상세로 간다. 폰 폭: 진행 세그먼트·액션 바 확인, 저장 버튼을 눌러 상태가 바뀌고 `/saved`에 반영되는지. 웹 폭: 3단·인용 블록·결과 표 확인. 원문 링크가 새 탭(`target="_blank" rel="noreferrer"`)으로 열리는지.

- [ ] **Step 3: 커밋**

```bash
git add apps/web
git commit -m "web: /paper/[id] 카드 상세(폰)와 정독(웹)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 8: `/paper/[id]/trust` — 신뢰도 근거

**Files:**
- Create: `apps/web/components/paper/TrustDetail.tsx`, `apps/web/app/(app)/paper/[id]/trust/page.tsx`

**Interfaces:**
- Consumes: `getPaperDetail`.
- Produces: `TrustDetail`: `{ assessment: Assessment; title: string }`.

- [ ] **Step 1: 구현**

`docs/DESIGN.md` §6 `/paper/[id]/trust` 명세대로:
- 뒤로 44×44(`aria-label="뒤로"`) + "신뢰도 근거"
- `verified-bg` 요약 카드: 통과한 단계 수("4단계 필터 전부 통과" 또는 "3단계 통과 · 1건 유의") + display 18px 한 문장 결론
- 1~4단계 각각 카드 한 장. `evidence`를 `stage`로 묶는다. `verdict: 'pass'`는 체크 원 + `surface`, `caution`은 경고 아이콘 + `caution-surface`/`caution-line`
- `caveats`가 있으면 유의점 목록
- 하단: "이 판정은 AI 보조 의견입니다. 최종 판단은 읽는 사람의 몫으로 남겨 둡니다." + `판정에 이의 제기` 버튼 — 누르면 "접수 창구는 준비 중입니다" 안내를 보여주는 `'use client'` 토글. 테이블에 쓰지 않는다.

**점수 숫자를 노출하지 않는다.** `stage3`의 `Score.value`는 화면에 쓰지 말고 `reason` 문장만 쓴다.

- [ ] **Step 2: 검증**

`pnpm --filter @jogan/web typecheck` 후 브라우저에서 검증 논문 하나와 `심사 전` 논문(ReviewBench) 둘 다 확인. 심사 전 논문은 2단계가 `caution`이어야 한다.

- [ ] **Step 3: 커밋**

```bash
git add apps/web
git commit -m "web: /paper/[id]/trust 신뢰도 근거 (단계별 근거 문장)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 9: `/saved` — 저장함

**Files:**
- Create: `apps/web/components/brief/FollowUpNote.tsx`, `apps/web/app/(app)/saved/page.tsx`

**Interfaces:**
- Consumes: `listSaved`, 서버 액션 `markPaperRead`, `toggleSave`.
- Produces: `FollowUpNote`: `{ followUp: FollowUp }` — `verified-surface` 박스 + 체크 아이콘 + 문구.

- [ ] **Step 1: 구현**

`docs/DESIGN.md` §6 `/saved` 명세대로: "저장함" + "N편 · 이번 주 M편", 세그먼트 `읽을 것 N` / `읽음 M`(`SegmentedControl`, `readAt` 유무로 필터), `verified-surface` 배너("회사 가서 이어보기 — PC 웹으로 오늘 저장한 N편이 넘어갑니다"; Zotero 문구는 연동이 없으므로 빼거나 "준비 중"으로), 저장 카드(배지 + 저장일 + display 16.5px 제목 + `요약 다시 보기`), 후속 소식 블록.

세그먼트 전환은 클라이언트 상태로 하되 데이터는 서버에서 한 번 받아 필터링한다(추가 왕복 없음).

- [ ] **Step 2: 검증**

typecheck 후 브라우저. 시드에는 TMR 논문 1편이 `followUp: accepted`로 저장돼 있어야 하고 "그 뒤 Sleep에 채택되었습니다"가 보여야 한다. `/paper/[id]`에서 저장을 토글하면 목록이 즉시 갱신되는지(`revalidatePath`).

- [ ] **Step 3: 커밋**

```bash
git add apps/web
git commit -m "web: /saved 저장함과 후속 소식

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 10: `/onboarding` — 관심사 등록

**Files:**
- Create: `apps/web/app/onboarding/page.tsx`, `apps/web/components/onboarding/OnboardingForm.tsx`, `apps/web/lib/actions/interests.ts`
- Modify: `packages/core/src/user.ts` 아래 스키마 추가

**Interfaces:**
- Produces:
  - core: `OnboardingInput = z.object({ labels: z.array(z.string().min(1)).min(1).max(5), departureTime: <HH:mm regex>, papersPerDay: z.number().int().min(1).max(5), includePreprints: z.boolean() })`
  - `interests.ts` 서버 액션: `submitOnboarding(formData: FormData): Promise<void>` — `OnboardingInput.parse()`로 검증하고 `createInterests` + `upsertSettings` 후 `redirect('/')`. 실패는 throw하지 말고 `{ error }`를 반환해 폼에 표시한다(`useActionState`).
  - `OnboardingForm`: `'use client'` — 추천 주제 칩 6개 토글, 자연어 입력, 출발 시각 `<input type="time">`, 편수 `Stepper`, 프리프린트 `Switch`, 하단 고정 CTA 54px.

- [ ] **Step 1: 구현**

`docs/DESIGN.md` §6 `/onboarding` 명세대로. 추천 주제 칩 6개는 컴포넌트 안에 상수로 둔다(`LLM 에이전트` `수면과 기억` `코드 리뷰 자동화` `단백질 구조 예측` `인과추론` `강화학습`). 선택 시 `verified` 배경 + `paper` 텍스트, 미선택 시 `surface` + `line` 테두리, 높이 44px. 칩은 실제 `<button type="button" aria-pressed>`.

관심사가 이미 있으면 페이지 상단에서 `redirect('/')`.

`(app)` 그룹 밖에 두어 탭·상단바 없이 전체 화면으로 뜬다. 레이아웃이 없으므로 이 페이지가 직접 `requireUser()`를 호출한다.

- [ ] **Step 2: 검증**

```bash
docker compose exec db psql -U jogan -d jogan -c "delete from interests"
pnpm dev
```
`/`로 가면 `/onboarding`으로 튕기고, 폼을 채워 제출하면 `/`로 오며 관심사가 보인다. 그 다음 `/onboarding`으로 직접 가면 `/`로 되돌아온다. 확인 후 `pnpm db:seed`로 관심사를 복구한다.

- [ ] **Step 3: 커밋**

```bash
git add apps/web packages/core
git commit -m "web: /onboarding 관심사 등록과 서버 액션

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 11: `/audio` — 오디오 브리핑 (다크, 재생 비활성)

**Files:**
- Create: `apps/web/app/(app)/audio/page.tsx`

**Interfaces:**
- Consumes: `getTodayBrief` / `getLatestBrief`.

- [ ] **Step 1: 구현**

`docs/DESIGN.md` §6 `/audio` 명세대로 다크 화면(`night-*` 토큰). 접기 화살표 + "오디오 브리핑" 라벨, "N번째 논문 · M편 중"(`night-accent`), display 27px 제목, 현재 문장 박스(`night-surface`), 진행 바 4px, 컨트롤(속도 / 이전 / 일시정지 76px 원형 / 다음 / 카드로 보기), 오늘의 순서 목록, 하단 안내.

**재생 컨트롤은 전부 `disabled`** 이고 하단 안내를 "오디오 브리핑은 준비 중입니다 — 화면은 완성된 모습입니다"로 바꾼다. 가짜 진행 바 애니메이션을 만들지 않는다(진행 0%). `카드로 보기`만 실제 링크로 동작한다.

`audioUrl`이 `null`이므로 시간 표시는 `--:--`.

이 페이지는 `(app)` 안에 있지만 다크 전체 화면이므로 하단 탭을 가린다 — 페이지 루트에 `fixed inset-0 z-10 bg-night`를 주거나, `(app)/layout.tsx`에서 `/audio`일 때 탭을 숨긴다. **후자를 쓴다**(`usePathname`이 이미 `BottomTabs`에 있으므로 거기서 `pathname === '/audio'`면 `null`을 반환).

- [ ] **Step 2: 검증**

typecheck 후 브라우저에서 폰 폭으로 확인. 대비: `night-muted`(#8C8577)보다 밝은 회색을 본문에 쓰지 않았는지.

- [ ] **Step 3: 커밋**

```bash
git add apps/web
git commit -m "web: /audio 오디오 브리핑 화면 (재생은 준비 중)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 12: 스모크 테스트와 전체 검증

**Files:**
- Create: `apps/web/smoke.test.ts`
- Modify: `apps/web/package.json` (`test` 스크립트 추가)

**Interfaces:**
- 없음 (검증 전용).

- [ ] **Step 1: 스모크 테스트**

`apps/web/smoke.test.ts` — 로그인 없이 확인 가능한 것만 검사한다. 세션 쿠키를 위조하지 않는다.

```ts
import { describe, expect, it } from 'vitest'

const base = process.env.SMOKE_BASE_URL

describe.skipIf(!base)('라우트 스모크', () => {
  const protectedRoutes = ['/', '/saved', '/audio', '/onboarding']

  it.each(protectedRoutes)('%s 는 미로그인 시 /login으로 보낸다', async (path) => {
    const res = await fetch(`${base}${path}`, { redirect: 'manual' })
    expect(res.status).toBe(307)
    expect(res.headers.get('location')).toContain('/login')
  })

  it('/login 은 200이고 로그인 버튼이 있다', async () => {
    const res = await fetch(`${base}/login`)
    expect(res.status).toBe(200)
    expect(await res.text()).toContain('Google로 계속하기')
  })

  it('/api/auth/providers 는 google을 포함한다', async () => {
    const res = await fetch(`${base}/api/auth/providers`)
    expect(res.status).toBe(200)
    expect(await res.json()).toHaveProperty('google')
  })

  it('매니페스트가 뜬다', async () => {
    const res = await fetch(`${base}/manifest.webmanifest`)
    expect(res.status).toBe(200)
    expect(await res.json()).toMatchObject({ name: '조간 논문' })
  })
})
```

`apps/web/package.json`에 `"test": "vitest run"` 추가.

- [ ] **Step 2: 전체 검증**

```bash
docker compose up -d
pnpm db:seed
pnpm typecheck
pnpm test
pnpm build
pnpm --filter @jogan/web start &
sleep 5
SMOKE_BASE_URL=http://localhost:3000 pnpm --filter @jogan/web test
kill %1
git status --short
```
Expected: 전부 종료 코드 0, `git status`는 빈 출력.

- [ ] **Step 3: 로그인 상태 육안 확인**

`pnpm dev` 후 브라우저에서 8개 라우트를 폰 폭(390)과 웹 폭(1440)으로 한 번씩 본다. 체크리스트:
- 배지에 점수 숫자가 없다
- 심사 전 논문이 `caution`으로 구분된다
- 모든 카드에 원문 링크가 있다
- `AI 보조 의견` 문구가 `/paper/[id]/trust`에 있다
- 하단 탭·버튼이 44px 이상이다
- `/audio`는 컨트롤이 비활성이고 "준비 중"이 보인다

- [ ] **Step 4: 커밋**

```bash
git add apps/web
git commit -m "web: 라우트 스모크 테스트

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Self-Review

- **Spec coverage:** 쿼리 계층 → Task 1·2. 이월 항목(client.ts HMR·requireUser·API 401) → Task 2·3. 폰트 전환 → Task 3. 프리미티브·셸 → Task 4·5. 8개 라우트 → Task 5(셸)·6(`/`)·7(`/paper/[id]`)·8(trust)·9(saved)·10(onboarding)·11(audio). 빈 상태 → Task 6. 테스트(매퍼·쿼리·스모크) → Task 1·2·12. 결정 6개(섹션 요약 미확장·자리만·연속 기록 실계산·오디오 비활성·next/font·이의 제기 버튼만)는 각각 Task 7·6·6·11·3·8에 명시.
- **Placeholder scan:** 없음. "준비 중" 문구는 의도된 UI 텍스트다. Task 4의 나머지 아이콘·ui 컴포넌트와 Task 5~11의 픽셀 값은 `docs/DESIGN.md` §1~§6을 권위 있는 출처로 지목했다 — 레포 안 문서이고 구현자가 읽을 수 있다.
- **Type consistency:** `BriefView`/`PaperDetail`/`SavedView`가 Task 2에서 정의되고 Task 6~9에서 그 이름으로 쓰인다. `rowToPaperSummary`가 `PaperSummary`(Task 1)를 반환하고 `PaperCard`가 그것을 받는다. 서버 액션 `toggleSave`/`markPaperRead`는 Task 7에서 정의되고 Task 9에서 재사용된다. `sourceUrl`(Task 3)이 Task 6·7에서 쓰인다. `todayInSeoul`은 `@jogan/db`에서 한 번만 정의된다(`seed.ts`의 중복 제거는 Task 2 Step 3).
